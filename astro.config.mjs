// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Currently hosted on GitHub Pages as a project page. When you point a
  // real domain at this site, change `site` to it and delete the `base` line.
  site: 'https://bleedinghahahahah-collab.github.io',
  base: '/oscar-restaurant',
});
